# Application for shelter volunteers to manage their volunteer time

This application is a work in progress. It uses Flask server with a React JS client.

To run this code, first start the Flask server, using the instructions in the <code>server</code> directory

Then, run the client, using the instructions in the <code>client_app</code> directory

